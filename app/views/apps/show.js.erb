document.write('<div class="container"><%= form_for(App.new, remote: true) do |f| %>'
        + '<h2 class="form-signin-heading">Sign in for News letter</h2>'
        +'<label for="inputEmail" class="sr-only">Email address</label>'
        +'<input type="email" id="inputEmail" class="form-control" placeholder="Email address" required autofocus>'
        +'<label for="inputPassword" class="sr-only">Password</label>'
        +'<input type="text" id="inputText" class="form-control" placeholder="Full name" required>'
        +'</div> <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>'
      + '<% end %>    </div>'); 
